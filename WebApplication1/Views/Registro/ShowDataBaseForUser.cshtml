@model List<WebApplication1.Models.Usuario>

<div class="container">
    <table class="table table-bordered">
        <thead>
        <tr>

            <th>UserName</th>
            <th>PassWord</th>
            <th> action </th>
        </tr>    
        </thead>
        <tbody>

            @*O "Model" usado a baixo se refere ao model na pasta*@

            @for (int i =0; i< Model.Count(); i++) 
            {
            <tr>
                <td> @Model[i].Username  </td>
                <td> @Model[i].Passaword </td>

                <td>
                    <a href="@Url.Action("Delete", new {IdParametro = Model[i].ID})"> <i class="glyphicon glyphicon-trash"> </i></a>

                    @*O "No botão selecionado de editar o parametro carregado em editar é enviado para actionresult Editar e carrega os dados em tela*@
                    @*O "No botão selecionado de editar o parametro carregado em editar é enviado para actionresult Editar e carrega os dados em tela*@
                    <a href="@Url.Action("Editar", new {id = Model[i].ID})"> <i class="glyphicon glyphicon-edit"> </i> </a>

                </td>              
            </tr>


            }
        </tbody>

     </table>


</div>